#include "FSM.hpp"

FSM::FSM():
	m_state(State::NONE)
{

}

void FSM::setState(int _grid_id)
{
	/*GridTile target = m_grid->get(_grid_id);

	if (target.isTrees() == true)
		{
			m_state = State::GET_WOOD;
		}
	else if (target.isBuilding() == true)
		{
			m_state = State::BUILD;
		}
	else
		{
			m_state = State::MOVE;
		}
*/
}

